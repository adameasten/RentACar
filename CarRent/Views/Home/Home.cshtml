@model StartPageVM
@{
    ViewBag.Title = "Home";
}

<form asp-controller="car" asp-action="search">
    <label asp-for="City"></label>
    <input asp-for="City" />

    <label asp-for="StartDate"></label>
    <input asp-for="StartDate" type="date" />
    <select asp-for="StartingHour" asp-items="Model.TimeStamps"></select>

    <label asp-for="EndDate"></label>
    <input asp-for="EndDate" type="date" />
    <select asp-for="EndingHour" asp-items="Model.TimeStamps"></select>

    <input type="submit" value="Sök" />
</form>

@*<form asp-controller="car" asp-action="userlocation" id="locationform">
    <input id="latCord" type="hidden" asp-for="Latitude" />
    <input id="longCord" type="hidden" asp-for="Longitude" />
</form>
<button id="locationButton" type="button" class="btn btn-default" onclick="getLocation()">Use Actual Location</button>*@

<script type="text/javascript">
    function getLocation() {
        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(addLocation);
        }
    }

    function addLocation(position) {
        console.log(position)
        document.getElementById("latCord").value = position.coords.latitude;
        document.getElementById("longCord").value = position.coords.longitude;
        document.getElementById("locationform").submit();
    };
</script>
